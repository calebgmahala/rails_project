<% if session[:id].eql?(@user.id) %>
  <h1>Editing User</h1>

  <%= render 'form', user: @user %>

  <%= link_to 'Show', @user %> |
  <%= link_to 'Back', users_path %>
<% else %>
  <h1>Path Unknown</h1>
  <%= link_to 'People', users_path %>
<% end %>
